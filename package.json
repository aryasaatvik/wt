{
  "name": "wt",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Git worktree helper: create with gitignored-file sync and dep install, inspect, remove safely, reap merged lanes",
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build:release-artifacts": "bun scripts/build-release-artifacts.ts",
    "release": "bun scripts/release-artifacts.ts"
  },
  "dependencies": {
    "@opentui/core": "0.4.3"
  }
}
